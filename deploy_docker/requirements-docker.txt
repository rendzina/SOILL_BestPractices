# Runtime dependencies for the Chainlit deploy image (no scraper stack).
# traceloop-sdk is pinned before chainlit to avoid pip ResolutionTooDeep
# (Chainlit → literalai → OpenTelemetry instrumentation backtracking).
pymongo>=4.6.0
python-dotenv>=1.0.0
dnspython>=2.4.0
faiss-cpu>=1.13.0
numpy>=1.26.0
mistralai>=1.2.0,<3
traceloop-sdk==0.60.0
chainlit==2.11.0
